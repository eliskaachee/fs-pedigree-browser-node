<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %></title>
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/calendar.css' />
  </head>
  <body>
    <div id="main">
      <%- calendar[0].monthName %>
      <% for(var month in calendar) { %>
        <div class="month">
        <% for(var day in calendar[month].dates) { %>
          <div class="day">
          <% for(var event in calendar[month].dates[day]) { %>
            <%- calendar[month].dates[day][event].name %>
            <% if(calendar[month].dates[day][event].type === 'birth') { %>
              was born today in 
            <% } else if(calendar[month].dates[day][event].type === 'marriage') { %>
              were married today in 
            <% } else { %>
              died today in 
            <% } %>
            <%- calendar[month].dates[day][event].year %>
            <%- calendar[month].monthName %>
            <%- calendar[month].dates[day][event].day %>
          <% } %>
        </div> <!-- end of day -->
        <% } %>
      </div> <!-- end of month -->
      <% } %>
    </div>
  </body>
  </html>